# ── Core API ────────────────────────────────────────────────────────────────
fastapi==0.111.0
uvicorn[standard]==0.29.0
pydantic==2.7.1
pydantic-settings==2.2.1

# ── Auth ────────────────────────────────────────────────────────────────────
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4

# ── Cache / Pub-Sub ─────────────────────────────────────────────────────────
redis[hiredis]==5.0.4

# ── Task Queue ──────────────────────────────────────────────────────────────
celery==5.4.0
celery[redis]==5.4.0
flower==2.0.1          # Celery monitoring UI

# ── Database ────────────────────────────────────────────────────────────────
asyncpg==0.29.0        # Async PostgreSQL / TimescaleDB driver
sqlalchemy[asyncio]==2.0.30
alembic==1.13.1

# ── ML — Phase 3 ────────────────────────────────────────────────────────────
lightgbm==4.3.0
shap==0.45.0
scikit-learn==1.4.2
pandas==2.2.2
numpy==1.26.4
joblib==1.4.0

# ── ML — Phase 4 (Risk) ─────────────────────────────────────────────────────
arch==6.3.0            # GARCH models
scipy==1.13.0

# ── ML — Phase 5 (Sentiment) ────────────────────────────────────────────────
transformers==4.41.0
torch==2.3.0           # CPU build; replace with torch+cu121 for GPU
sentencepiece==0.2.0
mlflow==2.13.0

# ── Data feed — Phase 2 ─────────────────────────────────────────────────────
websockets==12.0       # Broker WebSocket feed client
confluent-kafka==2.4.0 # Kafka producer/consumer
httpx==0.27.0          # Async HTTP for REST feeds

# ── Portfolio Optimisation — Phase 6 ────────────────────────────────────────
PyPortfolioOpt==1.5.5      # Black-Litterman + HRP + MVO
cvxpy==1.5.0               # Convex optimisation solver for MVO fallback

# ── Claude API + RAG — Phase 7 ──────────────────────────────────────────────
anthropic==0.30.0          # Claude API SDK (supports streaming)
tiktoken==0.7.0            # Token counting for context management

# ── Observability — Phase 8 ─────────────────────────────────────────────────
prometheus-client==0.20.0
sentry-sdk[fastapi]==2.6.0

# ── Testing — Phase 8 ────────────────────────────────────────────────────────
pytest==8.2.2
pytest-asyncio==0.23.7
pytest-mock==3.14.0
httpx==0.27.0              # Also used as AsyncClient in tests

# ── Utilities ───────────────────────────────────────────────────────────────
python-dotenv==1.0.1
structlog==24.1.0
prometheus-fastapi-instrumentator==7.0.0

# ── Scraping / Technical Analysis — Phase 10 ────────────────────────────────
beautifulsoup4==4.12.3     # HTML parsing for IDX.co.id broker summary
lxml==5.2.2                # Fast HTML/XML parser backend for bs4

# ── Real IDX market data feed ───────────────────────────────────────────────
# curl_cffi is REQUIRED, not optional. Yahoo fingerprints the TLS handshake and
# answers plain httpx/requests clients with HTTP 429 regardless of request rate.
# This library replays a real Chrome handshake, which is the only reason the
# quote and chart endpoints respond at all.
curl_cffi==0.7.4
